%# Forward args to component 
<& $comp, %ARGS &>
<%init>
# Does component exist in required theme? Otherwise use default.
my $comp = -e "/theme/$spoejs{theme}/$w" ? "/theme/$spoejs{theme}/$w" 
	                                 : "/theme/default/$w";
</%init>
<%args>
$w => undef
</%args>