% foreach my $cat ( keys %count ) {
  <% $cat %> (<% $count{$cat} %>)<br/>
% }


<%init>
  # Todo add div. parameters
  my $SL = new Spoejs::StoryList( lang => $session{lang_handle} );
  my %count = $SL->count_stories( by => 'author' ); 
</%init>