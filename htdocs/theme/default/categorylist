% foreach my $cat ( keys %count ) {
  <% $cat %> (<% $count{$cat} %>)<br/>
% }


<%init>
  # Todo add div. parameters
  # XXX: better path handling
  my $SL = new Spoejs::StoryList( lang => $spoejs{lang_handle},
	 path=>"$ENV{DOCUMENT_ROOT}/users/$session{user}" );
  my %count = $SL->count_stories( by => 'category' ); 
</%init>