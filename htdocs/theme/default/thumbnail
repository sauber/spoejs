<img src="\
<& /htm/url:encode, "/$spoejs{channel}/pic.jpg", d => $d, m=> $size,
p=>$img->{file} &>" <% $img->html_imgsize( size => $size ) %>"/>\
<%init>
  my $path = $spoejs{channel_path} . "/" .$d;
  my $ML = new Spoejs::MediaList( path => $path );
  my $img = $ML->get( $type );
  return unless $img;
 # $img = $img->utf_filename() if $img;

</%init>
<%args>
 $size => '150'
 $d => undef
 $type => 'random'
</%args>
